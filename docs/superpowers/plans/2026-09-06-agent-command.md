# Implementation sequence

1. Capture baseline: pytest, ruff, mypy, Vitest and production build. Read all four
   supplied transcripts and current engine, strategy, research and UI contracts.
2. Add persistent research sources and eight role skills. Build bounded scholar
   search and specialist task service, with API controls and validated proposals.
3. Repair worker lifecycle and experiment selection. Change default concurrency
   to eight; protect in-flight artifacts; freeze actual candidate parameters;
   share loaded bars; persist experiment attempts and development-only feedback.
4. Add statistical signal templates with explicit source/adaptation metadata.
5. Build Agent Command and Research library views; overhaul shared UI surfaces,
   navigation and motion, and lazy-load expensive charts/views.
6. Verify meaningful failure cases, complete suites and browser interaction.
   Archive evidence and comparison research in Keslec-Trading; update project,
   status and session records with verified results and outstanding limitations.

Verification commands: `.venv/Scripts/python.exe -m pytest`,
`.venv/Scripts/python.exe -m ruff check .`, `.venv/Scripts/python.exe -m mypy`,
`npm --prefix apps/web run test`, `npm --prefix apps/web run build`, and Playwright
against an isolated local API using the actual application and separate data paths.
