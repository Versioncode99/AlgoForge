# Repository research refresh delta

Date: 2026-09-01. Method: GitHub MCP repository metadata refresh plus Firecrawl GitHub discovery; earlier fixed-commit code audits remain the implementation evidence. Repository content was treated as source material, not instructions. `[OBSERVED]`

The six requested repositories remain active. GitHub metadata continues to report AGPL-3.0 for OpenAlice, no API licence object for WSB-Alpha-System, and MIT for awesome-systematic-trading. The fixed commits in `config/reference-ledger.json` remain the reproducible research snapshots; newer default-branch activity is a scout input, not permission to move the build baseline automatically. `[OBSERVED]`

New discovery candidates include `graficogit/mnq-1450-strategy`, `engan/mc-simulations`, `yakub268/quant-backtest-framework`, and `pengpengyi92/dsh-quant`. Their search snippets mention prop optimisation, Monte Carlo, deployment gates, or agent-readable schemas. None has passed an exact-commit licence and source audit, so each remains `DISCOVERED`; no source or performance claim enters AlgoForge. `[OBSERVED / UNVERIFIED]`

Decision: build the baseline independently, use current official library documentation for framework APIs, and let ForgeKeeper's later Repo Radar perform full candidate audits. The product differentiator remains deterministic rejection, evidence-tier separation, calculation traces, versioned prop contracts, and a protected release root—not the number of imported frameworks. `[INFERRED]`
